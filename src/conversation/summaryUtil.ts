import ConversationEvent from "@/conversation/types/ConversationEvent";

export function summarizeEvents(_events:ConversationEvent[], _maxSummaryDurationMs:number):string {
  return 'So much happened!'; // TODO
}